/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "fakestoreapi.com",
            "www.flaticon.com",
           "icons8.com"
        ]
    },
};

export default nextConfig;
