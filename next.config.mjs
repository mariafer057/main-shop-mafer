/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "fakestoreapi.com",
            "https://www.flaticon.com"
        ]
    },
};

export default nextConfig;
